{
	"format_version": "1.8.0",
	"animations": {
		"animation.stringworm.slither": {
			"loop": true,
			"bones": {
				"one": {
					"position": {
						"vector": ["v.time = q.anim_time;Math.sin((v.time) * 200)", "v.t=query.anim_time * 5;math.max(0, math.sin(v.t*90) * 1.5)", 0]
					}
				},
				"two": {
					"position": {
						"vector": ["v.time = q.anim_time - 0.1;Math.sin((v.time) * 200)", "v.t=query.anim_time * 5 - 0.75;math.max(0, math.sin(v.t*90) * 2.5)", 0]
					}
				},
				"three": {
					"position": {
						"vector": ["v.time = q.anim_time - 0.2;Math.sin((v.time) * 200)", "v.t=query.anim_time * 5 - 1.5;math.max(0, math.sin(v.t*90) * 2.5)", 0]
					}
				},
				"four": {
					"position": {
						"vector": ["v.time = q.anim_time - 0.3;Math.sin((v.time) * 200)", "v.t=query.anim_time * 5 - 2.25;math.max(0, math.sin(v.t*90) * 1.5)", 0]
					}
				},
				"five": {
					"position": {
						"vector": ["v.time = q.anim_time - 0.4;Math.sin((v.time) * 200)", "v.t=query.anim_time * 5 - 3;math.max(0, math.sin(v.t*90) * 1.5)", 0]
					}
				}
			}
		}
	},
	"geckolib_format_version": 2
}