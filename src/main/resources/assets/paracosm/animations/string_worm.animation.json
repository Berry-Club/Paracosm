{
	"format_version": "1.8.0",
	"animations": {
		"animation.stringworm.slither": {
			"loop": true,
			"bones": {
				"one": {
					"position": {
						"vector": [
							"v.time = query.anim_time;math.sin((v.time) * 200)",
							"v.t=math.mod(math.max(query.anim_time-1.0, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				},
				"two": {
					"position": {
						"vector": [
							"v.time = query.anim_time - 0.1;math.sin((v.time) * 200)",
							"v.t=math.mod(math.max(query.anim_time-1.25, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				},
				"three": {
					"position": {
						"vector": [
							"v.time = query.anim_time - 0.2;math.sin((v.time) * 200)",
							"v.t=math.mod(math.max(query.anim_time-1.5, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				},
				"four": {
					"position": {
						"vector": [
							"v.time = query.anim_time - 0.3;math.sin((v.time) * 200)",
							"v.t=math.mod(math.max(query.anim_time-1.75, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				},
				"five": {
					"position": {
						"vector": [
							"v.time = query.anim_time - 0.4;math.sin((v.time) * 200)",
							"v.t=math.mod(math.max(query.anim_time-2.0, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				}
			}
		}
	},
	"geckolib_format_version": 2
}