{
	"format_version": "1.8.0",
	"animations": {
		"animation.stringworm.slither": {
			"loop": true,
			"bones": {
				"one": {
					"position": {
						"vector": [
							"math.sin((q.anim_time) * 200)",
							"math.mod(math.max(q.anim_time - 1.0, 0), 2) >= 0.5 ? 0 : math.sin(math.mod(math.mod(math.max(q.anim_time - 1.0, 0), 2), 0.5) / 0.5 * 180)\n",
							0
						]
					}
				},
				"two": {
					"position": {
						"vector": [
							"math.sin((q.anim_time - 0.1) * 200)",
							"v.t=math.mod(math.max(q.anim_time-1.25, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				},
				"three": {
					"position": {
						"vector": [
							"math.sin((q.anim_time - 0.2) * 200)",
							"v.t=math.mod(math.max(q.anim_time-1.5, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				},
				"four": {
					"position": {
						"vector": [
							"math.sin((q.anim_time - 0.3) * 200)",
							"v.t=math.mod(math.max(q.anim_time-1.75, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				},
				"five": {
					"position": {
						"vector": [
							"math.sin((q.anim_time - 0.4) * 200)",
							"v.t=math.mod(math.max(q.anim_time-2.0, 0), 2);v.hop=0.5;v.t >= v.hop ? 0 : math.sin(math.mod(v.t, v.hop) / v.hop * 180)",
							0
						]
					}
				}
			}
		}
	},
	"geckolib_format_version": 2
}